<view class="nav-bar">
    <view class="item {{item.isActive ?'active' :''}}" wx:for="{{titles}}" wx:key="id">
      <text bind:tap="handleitemClick" data-index="{{index}}">{{item.title}}</text>
    </view>
</view>
